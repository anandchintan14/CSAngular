import { MaintenanceTransaction } from './maintenance-transaction';

describe('MaintenanceTransaction', () => {
  it('should create an instance', () => {
    expect(new MaintenanceTransaction()).toBeTruthy();
  });
});
